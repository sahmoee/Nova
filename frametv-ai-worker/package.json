{
  "name": "frametv-ai-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker bridging the FrameTV app to the Anthropic API for AI features.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
