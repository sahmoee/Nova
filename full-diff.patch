diff --git a/Nova/App/SettingsStore.swift b/Nova/App/SettingsStore.swift
index 78c5aaf..c83f66c 100644
--- a/Nova/App/SettingsStore.swift
+++ b/Nova/App/SettingsStore.swift
@@ -592,6 +592,13 @@ final class SettingsStore: ObservableObject {
         if let v = cloud.double(forKey: Key.textSizeBoost), v > 0, textSizeBoost != v {
             textSizeBoost = v
         }
+        // FIX: pinned collections were pushed to iCloud in didSet but never merged
+        // back here, so pins made on one device never appeared on the others.
+        if let data = cloud.data(forKey: Key.pinnedCollections),
+           let pins = try? JSONDecoder().decode([String].self, from: data),
+           pinnedCollections != pins {
+            pinnedCollections = pins
+        }
     }
 
     /// Like streamPreferences, but also biases ranking by the user's addon order
diff --git a/Nova/App/Theme.swift b/Nova/App/Theme.swift
index 397083e..cfc0def 100644
--- a/Nova/App/Theme.swift
+++ b/Nova/App/Theme.swift
@@ -195,6 +195,15 @@ enum Theme {
         static var y: CGFloat { Theme.scaled(10, min: 5) }
     }
 
+    // MARK: - Motion
+
+    /// Shared animation constants so entrances/exits feel consistent app-wide.
+    /// Additive only — existing per-site animations are unchanged.
+    enum Motion {
+        /// Standard springy show/hide for panels, prompts, and the mini player bar.
+        static let spring = SwiftUI.Animation.spring(response: 0.38, dampingFraction: 0.82)
+    }
+
     // MARK: - Spacing
 
     enum Spacing {
@@ -326,38 +335,33 @@ struct WrapFlowLayout: Layout {
     /// leading so existing callers are unaffected; pass .center to center rows.
     var alignment: HorizontalAlignment = .leading
 
-    func sizeThatFits(proposal: ProposedViewSize, subviews: Subviews, cache: inout ()) -> CGSize {
-        let maxWidth = proposal.width ?? .infinity
-        var rowWidth: CGFloat = 0
-        var rowHeight: CGFloat = 0
-        var totalHeight: CGFloat = 0
-        var totalWidth: CGFloat = 0
-        for view in subviews {
-            let size = view.sizeThatFits(.unspecified)
-            if rowWidth > 0, rowWidth + spacing + size.width > maxWidth {
-                totalWidth = Swift.max(totalWidth, rowWidth)
-                totalHeight += rowHeight + lineSpacing
-                rowWidth = size.width
-                rowHeight = size.height
-            } else {
-                rowWidth += (rowWidth > 0 ? spacing : 0) + size.width
-                rowHeight = Swift.max(rowHeight, size.height)
-            }
-        }
-        totalWidth = Swift.max(totalWidth, rowWidth)
-        totalHeight += rowHeight
-        return CGSize(width: min(totalWidth, maxWidth), height: totalHeight)
+    // REGRESSION GUARD — read before changing `wrapWidth`.
+    //
+    // A VStack sizes itself from its children's *ideal* widths, which it discovers by
+    // probing them with an unspecified (nil) proposal. If this layout answers that
+    // probe with "every chip on one line", that single-line total becomes the parent
+    // column's width, and the whole block around it (description, year, the chip row
+    // itself) is laid out wider than the screen and clipped on both edges. That is the
+    // off-screen symptom this rail kept regressing to, and why fixing it at the call
+    // site never held — the cause is here, in the layout, not at the call site.
+    //
+    // So: only wrap at the proposed width when that width is real and finite. For an
+    // unspecified or infinite proposal, report the widest *single* subview — a width
+    // that always fits on any screen. The real layout pass gets a concrete width in
+    // `placeSubviews` and fills it normally, so nothing looks narrow in practice.
+
+    /// The width rows should wrap at, given a (possibly absent) proposed width.
+    private static func wrapWidth(_ proposed: CGFloat?, sizes: [CGSize]) -> CGFloat {
+        if let proposed, proposed > 0, proposed.isFinite { return proposed }
+        return sizes.map(\.width).max() ?? 0
     }
 
-    func placeSubviews(in bounds: CGRect, proposal: ProposedViewSize, subviews: Subviews, cache: inout ()) {
-        let maxWidth = bounds.width
-
-        // Group subviews into rows first, so each row can be centered if requested.
+    /// Groups subviews into rows that each fit within `maxWidth`.
+    private func rows(for sizes: [CGSize], maxWidth: CGFloat) -> [[Int]] {
         var rows: [[Int]] = []
         var current: [Int] = []
         var rowWidth: CGFloat = 0
-        for (i, view) in subviews.enumerated() {
-            let size = view.sizeThatFits(.unspecified)
+        for (i, size) in sizes.enumerated() {
             if !current.isEmpty, rowWidth + spacing + size.width > maxWidth {
                 rows.append(current)
                 current = [i]
@@ -368,15 +372,41 @@ struct WrapFlowLayout: Layout {
             }
         }
         if !current.isEmpty { rows.append(current) }
+        return rows
+    }
+
+    func sizeThatFits(proposal: ProposedViewSize, subviews: Subviews, cache: inout ()) -> CGSize {
+        let sizes = subviews.map { $0.sizeThatFits(.unspecified) }
+        guard !sizes.isEmpty else { return .zero }
+        let maxWidth = Self.wrapWidth(proposal.width, sizes: sizes)
+
+        var totalWidth: CGFloat = 0
+        var totalHeight: CGFloat = 0
+        for (index, row) in rows(for: sizes, maxWidth: maxWidth).enumerated() {
+            let rowSizes = row.map { sizes[$0] }
+            let rowW = rowSizes.reduce(0) { $0 + $1.width } + spacing * CGFloat(Swift.max(row.count - 1, 0))
+            totalWidth = Swift.max(totalWidth, rowW)
+            totalHeight += (rowSizes.map(\.height).max() ?? 0) + (index > 0 ? lineSpacing : 0)
+        }
+        // Never report more than we were allowed to use.
+        return CGSize(width: Swift.min(totalWidth, maxWidth), height: totalHeight)
+    }
+
+    func placeSubviews(in bounds: CGRect, proposal: ProposedViewSize, subviews: Subviews, cache: inout ()) {
+        let sizes = subviews.map { $0.sizeThatFits(.unspecified) }
+        guard !sizes.isEmpty else { return }
+        // Use the real laid-out width; fall back the same way `sizeThatFits` does if
+        // we are somehow handed a degenerate (zero/infinite) bounds width.
+        let maxWidth = Self.wrapWidth(bounds.width, sizes: sizes)
 
         var y = bounds.minY
-        for row in rows {
-            let sizes = row.map { subviews[$0].sizeThatFits(.unspecified) }
-            let rowW = sizes.reduce(0) { $0 + $1.width } + spacing * CGFloat(max(row.count - 1, 0))
-            let rowHeight = sizes.map(\.height).max() ?? 0
-            var x = bounds.minX + (alignment == .center ? max(0, (maxWidth - rowW) / 2) : 0)
+        for row in rows(for: sizes, maxWidth: maxWidth) {
+            let rowSizes = row.map { sizes[$0] }
+            let rowW = rowSizes.reduce(0) { $0 + $1.width } + spacing * CGFloat(Swift.max(row.count - 1, 0))
+            let rowHeight = rowSizes.map(\.height).max() ?? 0
+            var x = bounds.minX + (alignment == .center ? Swift.max(0, (maxWidth - rowW) / 2) : 0)
             for (idx, subviewIndex) in row.enumerated() {
-                let size = sizes[idx]
+                let size = rowSizes[idx]
                 subviews[subviewIndex].place(at: CGPoint(x: x, y: y), anchor: .topLeading,
                                              proposal: ProposedViewSize(size))
                 x += size.width + spacing
diff --git a/Nova/Components/CatalogShelfRow.swift b/Nova/Components/CatalogShelfRow.swift
index 530cb2b..0747be8 100644
--- a/Nova/Components/CatalogShelfRow.swift
+++ b/Nova/Components/CatalogShelfRow.swift
@@ -73,7 +73,8 @@ struct CatalogShelfRow: View {
                     .font(.appFont(12, weight: .bold))
                     .foregroundStyle(Theme.Colors.textTertiary)
                     .padding(.horizontal, 8).padding(.vertical, 3)
-                    .background(Color.white.opacity(0.08), in: Capsule())
+                    // Use the shared card color instead of a magic opacity value.
+                    .background(Theme.Colors.card, in: Capsule())
             }
             Spacer()
         }
diff --git a/Nova/Components/FeaturedHero.swift b/Nova/Components/FeaturedHero.swift
index 27b498d..f2c6eb6 100644
--- a/Nova/Components/FeaturedHero.swift
+++ b/Nova/Components/FeaturedHero.swift
@@ -50,7 +50,8 @@ struct FeaturedHero: View {
             CachedAsyncImage(url: item.backdropURL ?? item.posterURL, maxPixel: 1600) { image in
                 image.resizable().aspectRatio(contentMode: .fill)
             } placeholder: {
-                Rectangle().fill(Theme.Colors.card)
+                // Shimmer while the backdrop loads, matching the app's other skeletons.
+                Rectangle().fill(Theme.Colors.card).shimmering()
             }
             .frame(width: width, height: heroHeight)
             .clipped()
diff --git a/Nova/Components/MediaCard.swift b/Nova/Components/MediaCard.swift
index d3ce076..cabbdae 100644
--- a/Nova/Components/MediaCard.swift
+++ b/Nova/Components/MediaCard.swift
@@ -278,5 +278,9 @@ struct CatalogPosterCard: View {
                 .lineLimit(1)
                 .frame(width: width, alignment: .leading)
         }
+        // Larger tap target: the whole card (including the gap under the poster)
+        // is tappable, and VoiceOver reads it as one element.
+        .contentShape(Rectangle())
+        .accessibilityElement(children: .combine)
     }
 }
diff --git a/Nova/Components/SourceCard.swift b/Nova/Components/SourceCard.swift
index 1d640df..0016ff4 100644
--- a/Nova/Components/SourceCard.swift
+++ b/Nova/Components/SourceCard.swift
@@ -94,6 +94,10 @@ struct SourceCard: View {
         }
         .buttonStyle(.pressable)
         .focused($focused)
+        // Accessibility: speak the card as one element — name plus connection status.
+        .accessibilityElement(children: .ignore)
+        .accessibilityLabel("\(title), \(statusLine)")
+        .accessibilityAddTraits(.isButton)
         .scaleEffect(focused ? Theme.CardSize.focusScale : 1.0)
         .animation(.easeOut(duration: 0.16), value: focused)
     }
diff --git a/Nova/Components/UniversalSearchView.swift b/Nova/Components/UniversalSearchView.swift
index 47429d6..9aaabc7 100644
--- a/Nova/Components/UniversalSearchView.swift
+++ b/Nova/Components/UniversalSearchView.swift
@@ -127,6 +127,8 @@ struct UniversalSearchView: View {
                         .foregroundStyle(Theme.Colors.textTertiary)
                 }
                 .novaIconStyle()
+                // Accessibility: icon-only clear button needs a spoken name.
+                .accessibilityLabel("Clear search")
             }
             Button {
                 Task { await runAISearch() }
@@ -136,6 +138,8 @@ struct UniversalSearchView: View {
                     .foregroundStyle(aiSearching ? Theme.Colors.textTertiary : Theme.Colors.accent)
             }
             .novaIconStyle()
+            // Accessibility: name the sparkles icon so its purpose is clear.
+            .accessibilityLabel("AI search")
             .disabled(aiSearching || query.trimmingCharacters(in: .whitespaces).count < 2)
         }
         .padding(Theme.Spacing.md)
diff --git a/Nova/Models/RealDebridModels.swift b/Nova/Models/RealDebridModels.swift
index 3076a10..46b7a23 100644
--- a/Nova/Models/RealDebridModels.swift
+++ b/Nova/Models/RealDebridModels.swift
@@ -21,11 +21,17 @@ struct RealDebridUser: Codable, Hashable {
 
     var isPremium: Bool { type.lowercased() == "premium" }
 
+    // FIX: cache the formatter instead of allocating a new ISO8601DateFormatter on
+    // every property access (it is expensive to create and thread-safe to share).
+    // ISO8601DateFormatter isn't `Sendable`, and this formatter is configured once
+    // and only ever read from here, so opt out of the concurrency check the same
+    // way the app does for other shared statics (see PlayerMemory / Theme).
+    nonisolated(unsafe) private static let expirationFormatter = ISO8601DateFormatter()
+
     /// Parsed expiration date, if present and well-formed.
     var expirationDate: Date? {
         guard let expiration else { return nil }
-        let f = ISO8601DateFormatter()
-        return f.date(from: expiration)
+        return Self.expirationFormatter.date(from: expiration)
     }
 }
 
diff --git a/Nova/Services/AddonStore.swift b/Nova/Services/AddonStore.swift
index 79c956b..ef58bf1 100644
--- a/Nova/Services/AddonStore.swift
+++ b/Nova/Services/AddonStore.swift
@@ -84,6 +84,10 @@ final class AddonStore: ObservableObject {
         guard let data = CloudSync.shared.data(forKey: Self.cloudKey),
               let cloudAddons = try? decoder.decode([InstalledAddon].self, from: data) else { return }
         // Last-write-wins by adopting the cloud set when it's non-empty.
+        // FIX: enforce the documented "non-empty" condition. An empty cloud list
+        // (e.g. pushed by a fresh device before seeding) previously wiped every
+        // locally installed addon.
+        guard !cloudAddons.isEmpty else { return }
         if cloudAddons != addons {
             addons = cloudAddons
             // Persist locally without re-pushing identical data to the cloud.
diff --git a/Nova/Services/BackupManager.swift b/Nova/Services/BackupManager.swift
index a13b38b..4766258 100644
--- a/Nova/Services/BackupManager.swift
+++ b/Nova/Services/BackupManager.swift
@@ -128,6 +128,13 @@ final class BackupManager: ObservableObject {
 
     private let keychain = KeychainStore.shared
 
+    // FIX: one shared formatter instead of allocating a fresh ISO8601DateFormatter
+    // at three call sites (auto-sync stamp, export filename, share-code expiry).
+    // ISO8601DateFormatter is expensive to create and thread-safe to reuse. It isn't
+    // `Sendable`, and this instance is never reconfigured, so opt out of the
+    // concurrency check the same way the app does for other shared statics.
+    nonisolated(unsafe) private static let isoFormatter = ISO8601DateFormatter()
+
     init() {
         if let snap = loadSnapshotFromCloud() {
             lastBackupDate = snap.createdAt
@@ -240,7 +247,7 @@ final class BackupManager: ObservableObject {
         CloudSync.shared.pull()
         guard let snap = loadSnapshotFromCloud() else { return false }
         let defaults = UserDefaults.standard
-        let stamp = ISO8601DateFormatter().string(from: snap.createdAt)
+        let stamp = Self.isoFormatter.string(from: snap.createdAt)
         if snap.deviceName == deviceName(), defaults.string(forKey: lastAutoSyncKey) == nil {
             defaults.set(stamp, forKey: lastAutoSyncKey)
             return false
@@ -394,7 +401,7 @@ final class BackupManager: ObservableObject {
 
         guard let data = try? JSONEncoder().encode(snap) else { return nil }
 
-        let stamp = ISO8601DateFormatter().string(from: snap.createdAt)
+        let stamp = Self.isoFormatter.string(from: snap.createdAt)
             .replacingOccurrences(of: ":", with: "-")
         let url = FileManager.default.temporaryDirectory
             .appendingPathComponent("Nova-\(stamp).nova")
@@ -489,7 +496,7 @@ final class BackupManager: ObservableObject {
                 return nil
             }
             let decoded = try JSONDecoder().decode(ShareCreateResponse.self, from: respData)
-            let expires = decoded.expiresAt.flatMap { ISO8601DateFormatter().date(from: $0) }
+            let expires = decoded.expiresAt.flatMap { Self.isoFormatter.date(from: $0) }
             // Full share code = lookup code + "~" + base64(key). The "~" separator is
             // outside the base64 alphabet, and the lookup code is A-Z0-9 only.
             let keyB64 = key.withUnsafeBytes { Data($0) }.base64EncodedString()
diff --git a/Nova/Services/DirectURLService.swift b/Nova/Services/DirectURLService.swift
index 8bfb6b9..6b4a3fb 100644
--- a/Nova/Services/DirectURLService.swift
+++ b/Nova/Services/DirectURLService.swift
@@ -31,7 +31,13 @@ actor DirectURLService {
             guard let http = response as? HTTPURLResponse else { return url }
 
             // Reject obvious client/server errors.
-            guard (200...399).contains(http.statusCode) else {
+            // FIX: 405/501 mean the server rejects the HEAD *method*, not that the
+            // URL is bad — the fallback comment below documents that intent, but
+            // these statuses previously fell into `unreachable`, blocking valid
+            // URLs on servers that only accept GET. Let them through; the player
+            // surfaces a real error if the URL truly fails.
+            guard (200...399).contains(http.statusCode)
+                    || http.statusCode == 405 || http.statusCode == 501 else {
                 throw URLValidationError.unreachable
             }
 
diff --git a/Nova/Services/EPGService.swift b/Nova/Services/EPGService.swift
index 20f5a82..276d421 100644
--- a/Nova/Services/EPGService.swift
+++ b/Nova/Services/EPGService.swift
@@ -33,6 +33,12 @@ actor EPGService {
         else { return }
         let parsed = XMLTVParser.parse(data)
         guard !parsed.isEmpty else { return }
+        // FIX: replace (not append to) the programme lists for channels covered by
+        // this guide. Re-loading after the TTL expired previously appended the same
+        // programmes again, so lists grew with duplicates on every refresh.
+        for key in Set(parsed.map(\.channelID)) {
+            programmesByChannel[key] = []
+        }
         for programme in parsed {
             programmesByChannel[programme.channelID, default: []].append(programme)
         }
diff --git a/Nova/Services/LibraryEnricher.swift b/Nova/Services/LibraryEnricher.swift
index fa1098c..b6bca7d 100644
--- a/Nova/Services/LibraryEnricher.swift
+++ b/Nova/Services/LibraryEnricher.swift
@@ -79,6 +79,9 @@ final class LibraryEnricher: ObservableObject {
         var req = URLRequest(url: url)
         req.httpMethod = "POST"
         req.setValue("application/json", forHTTPHeaderField: "Content-Type")
+        // FIX: attach the Worker auth headers like every other Worker call does.
+        // Without them, a token-protected Worker rejects all AI title cleanups.
+        for (k, v) in AISearchService.authHeaders { req.setValue(v, forHTTPHeaderField: k) }
         req.httpBody = try? JSONEncoder().encode(["query": prompt])
         req.timeoutInterval = 20
         guard let (data, response) = try? await session.data(for: req),
diff --git a/Nova/Services/LibraryStore.swift b/Nova/Services/LibraryStore.swift
index 7aaf9a4..ab7190c 100644
--- a/Nova/Services/LibraryStore.swift
+++ b/Nova/Services/LibraryStore.swift
@@ -85,7 +85,10 @@ final class LibraryStore: ObservableObject {
         let cloudRev = CloudSync.shared.double(forKey: cloudRevisionKey) ?? 0
         let localRev = UserDefaults.standard.double(forKey: cloudRevisionKey)
         // Only adopt if the cloud copy is at least as new, and actually differs.
-        guard cloudRev >= localRev else { return }
+        // FIX: the "actually differs" half was never enforced, so every KVS
+        // notification republished the whole library (invalidating all observing
+        // views) and rewrote the local file even when nothing changed.
+        guard cloudRev >= localRev, decoded != items else { return }
 
         applyingRemoteChange = true
         items = decoded
diff --git a/Nova/Services/LiveTVSourceStore.swift b/Nova/Services/LiveTVSourceStore.swift
index f67e5b3..10a839b 100644
--- a/Nova/Services/LiveTVSourceStore.swift
+++ b/Nova/Services/LiveTVSourceStore.swift
@@ -98,6 +98,16 @@ final class LiveTVSourceStore: ObservableObject {
         ) { [weak self] _ in
             Task { @MainActor [weak self] in self?.reload() }
         }
+
+        // FIX: register the network-restored observer exactly once here. It used to be
+        // added inside mergeFromCloud(), which runs on init, reload, and every iCloud
+        // external change, stacking duplicate observers and duplicate refreshes.
+        NotificationCenter.default.addObserver(
+            forName: NetworkConditionMonitor.networkRestored,
+            object: nil, queue: .main
+        ) { [weak self] _ in
+            Task { @MainActor [weak self] in await self?.refreshAll() }
+        }
     }
 
     /// Re-reads the source list from disk (used after a backup restore), adopts any
@@ -122,10 +132,15 @@ final class LiveTVSourceStore: ObservableObject {
     ]
 
     private func seedBuiltInsIfNeeded() {
+        var didAppend = false
         for builtIn in Self.builtInSources where !sources.contains(where: { $0.name == builtIn.name && $0.isBuiltIn }) {
             sources.append(builtIn)
+            didAppend = true
         }
-        persist()
+        // FIX: persist only when a built-in was actually added. This ran on every
+        // init/reload/cloud merge and unconditionally re-pushed identical data to
+        // iCloud, triggering needless external-change churn on other devices.
+        if didAppend { persist() }
     }
 
     private func load() {
@@ -153,14 +168,8 @@ final class LiveTVSourceStore: ObservableObject {
             defaults.set(encoded, forKey: defaultsKey)
         }
         // Refresh channels for any enabled sources adopted from the cloud.
+        // (Network-restored observer registration moved to init() — see FIX there.)
         Task { await refreshAll() }
-        // When connectivity returns after an outage, reload stale playlists.
-        NotificationCenter.default.addObserver(
-            forName: NetworkConditionMonitor.networkRestored,
-            object: nil, queue: .main
-        ) { [weak self] _ in
-            Task { @MainActor [weak self] in await self?.refreshAll() }
-        }
     }
 
     func setEnabled(_ enabled: Bool, for source: LiveTVSource) {
@@ -225,6 +234,9 @@ final class LiveTVSourceStore: ObservableObject {
             let text = String(decoding: data, as: UTF8.self)
             channelsBySource[source.id] = Self.parseM3U(text)
             lastRefreshed[source.id] = Date()
+            // FIX: clear any stale error once a refresh succeeds, so the UI doesn't
+            // keep showing a failure banner after the source has recovered.
+            lastError = nil
         } catch {
             lastError = "Couldn't load \(source.name): \(error.localizedDescription)"
         }
diff --git a/Nova/Services/MetadataParser.swift b/Nova/Services/MetadataParser.swift
index acc20ea..90eb8c7 100644
--- a/Nova/Services/MetadataParser.swift
+++ b/Nova/Services/MetadataParser.swift
@@ -52,7 +52,9 @@ enum MetadataParser {
             if nums.count == 2 { return (nums[0], nums[1]) }
         }
         // 1x02
-        if let r = s.range(of: #"(\d{1,2})x(\d{1,3})"#, options: .regularExpression) {
+        // FIX: anchor with word boundaries so pixel dimensions like "1920x1080"
+        // aren't mis-parsed as season/episode (previously matched "20x108").
+        if let r = s.range(of: #"\b(\d{1,2})x(\d{1,3})\b"#, options: .regularExpression) {
             let match = String(s[r])
             let nums = match.split(separator: "x").compactMap { Int($0) }
             if nums.count == 2 { return (nums[0], nums[1]) }
diff --git a/Nova/Services/NowPlayingStore.swift b/Nova/Services/NowPlayingStore.swift
index a80c891..8ebaead 100644
--- a/Nova/Services/NowPlayingStore.swift
+++ b/Nova/Services/NowPlayingStore.swift
@@ -29,12 +29,16 @@ final class NowPlayingStore: ObservableObject {
     func begin(_ item: MediaItem, initialProgress: Double = 0) {
         current = item
         isPlaying = true
-        progress = max(0, min(1, initialProgress))
+        // FIX: guard non-finite values (position/duration math can yield NaN when a
+        // stream has no known duration); NaN slipped through min/max clamping and
+        // rendered a broken/full progress line.
+        progress = initialProgress.isFinite ? max(0, min(1, initialProgress)) : 0
         playerPresented = true
     }
 
     func update(progress: Double, isPlaying: Bool) {
-        self.progress = max(0, min(1, progress))
+        // FIX: same non-finite guard as begin(); keep the last good value on NaN.
+        if progress.isFinite { self.progress = max(0, min(1, progress)) }
         self.isPlaying = isPlaying
     }
 
diff --git a/Nova/Services/RealDebridClient.swift b/Nova/Services/RealDebridClient.swift
index a29014a..21963c6 100644
--- a/Nova/Services/RealDebridClient.swift
+++ b/Nova/Services/RealDebridClient.swift
@@ -94,8 +94,9 @@ final actor RealDebridClient {
         session: URLSession = AppNetworking.shared,
         tokenProvider: @escaping @Sendable () -> String? = { KeychainStore.shared.realDebridToken }
     ) {
-        let config = session.configuration
-        config.timeoutIntervalForRequest = 30
+        // FIX: mutating `session.configuration` here was a no-op — URLSession returns
+        // a COPY of its configuration, so the intended 30s timeout never applied.
+        // The timeout is now set per-request in makeRequest instead.
         self.session = session
         self.tokenProvider = tokenProvider
         self.decoder = JSONDecoder()
@@ -230,6 +231,9 @@ final actor RealDebridClient {
         let url = Self.baseURL.appendingPathComponent(String(endpoint.path.dropFirst()))
         var req = URLRequest(url: url)
         req.httpMethod = endpoint.method
+        // FIX: apply the 30s timeout per-request (see init — the old configuration
+        // mutation never took effect).
+        req.timeoutInterval = 30
         req.setValue("Bearer \(token)", forHTTPHeaderField: "Authorization")
 
         if let form, endpoint.method == "POST" {
diff --git a/Nova/Services/SMBStreamServer.swift b/Nova/Services/SMBStreamServer.swift
index ffcb719..153a247 100644
--- a/Nova/Services/SMBStreamServer.swift
+++ b/Nova/Services/SMBStreamServer.swift
@@ -82,10 +82,10 @@ actor SMBStreamServer {
         guard let request = await readRequest(conn) else { conn.cancel(); return }
 
         // Parse a Range header if present.
-        let (start, end) = parseRange(request, fileSize: fileSize)
+        let (start, end, hasRange) = parseRange(request, fileSize: fileSize)
         let length = end - start + 1
 
-        await sendResponse(conn, start: start, end: end, length: length)
+        await sendResponse(conn, start: start, end: end, length: length, isPartial: hasRange)
     }
 
     private func readRequest(_ conn: NWConnection) async -> String? {
@@ -100,44 +100,54 @@ actor SMBStreamServer {
         }
     }
 
-    private func parseRange(_ request: String, fileSize: Int64) -> (Int64, Int64) {
+    private func parseRange(_ request: String, fileSize: Int64) -> (Int64, Int64, Bool) {
         // Default: whole file.
         var start: Int64 = 0
         var end: Int64 = fileSize - 1
+        var hasRange = false
         for line in request.split(separator: "\r\n") {
             if line.lowercased().hasPrefix("range:") {
                 // e.g. "Range: bytes=12345-" or "bytes=0-1023"
                 if let eq = line.firstIndex(of: "=") {
+                    hasRange = true
                     let spec = line[line.index(after: eq)...]
                     let parts = spec.split(separator: "-", maxSplits: 1, omittingEmptySubsequences: false)
                     if let s = parts.first, let sv = Int64(s.trimmingCharacters(in: .whitespaces)) {
                         start = sv
-                    }
-                    if parts.count > 1, let ev = Int64(parts[1].trimmingCharacters(in: .whitespaces)) {
-                        end = ev
+                        if parts.count > 1, let ev = Int64(parts[1].trimmingCharacters(in: .whitespaces)) {
+                            end = ev
+                        }
+                    } else if parts.count > 1,
+                              let suffix = Int64(parts[1].trimmingCharacters(in: .whitespaces)) {
+                        // FIX: suffix ranges ("bytes=-N" = the LAST N bytes) were
+                        // previously served from the start of the file, corrupting
+                        // AVPlayer's tail probes (e.g. reading the MP4 moov atom).
+                        start = max(0, fileSize - suffix)
+                        end = fileSize - 1
                     }
                 }
             }
         }
         start = max(0, min(start, fileSize - 1))
         end = max(start, min(end, fileSize - 1))
-        return (start, end)
+        return (start, end, hasRange)
     }
 
-    private func sendResponse(_ conn: NWConnection, start: Int64, end: Int64, length: Int64) async {
+    private func sendResponse(_ conn: NWConnection, start: Int64, end: Int64, length: Int64, isPartial: Bool) async {
         guard let client else { conn.cancel(); return }
 
-        // HTTP headers: 206 Partial Content with the requested range.
-        let header = """
-        HTTP/1.1 206 Partial Content\r
-        Content-Type: video/mp4\r
-        Accept-Ranges: bytes\r
-        Content-Length: \(length)\r
-        Content-Range: bytes \(start)-\(end)/\(fileSize)\r
-        Connection: close\r
-        \r
-
-        """
+        // FIX: only answer 206 (with a Content-Range) for actual Range requests; a
+        // plain GET must get 200 OK per HTTP semantics — some client stacks reject
+        // an unsolicited 206.
+        let statusLine = isPartial ? "HTTP/1.1 206 Partial Content" : "HTTP/1.1 200 OK"
+        let rangeHeader = isPartial ? "Content-Range: bytes \(start)-\(end)/\(fileSize)\r\n" : ""
+        let header = "\(statusLine)\r\n"
+            + "Content-Type: video/mp4\r\n"
+            + "Accept-Ranges: bytes\r\n"
+            + "Content-Length: \(length)\r\n"
+            + rangeHeader
+            + "Connection: close\r\n"
+            + "\r\n"
         conn.send(content: header.data(using: .utf8), completion: .contentProcessed { _ in })
 
         // Stream the requested byte range from SMB. AMSMB2 returns an async stream
diff --git a/Nova/Services/StreamRanker.swift b/Nova/Services/StreamRanker.swift
index 17040d5..372f366 100644
--- a/Nova/Services/StreamRanker.swift
+++ b/Nova/Services/StreamRanker.swift
@@ -264,7 +264,10 @@ enum StreamRanker {
 
     /// Parses seeders like "👤 1234", "Seeders: 56", "S:56".
     static func parseSeeders(from s: String) -> Int? {
-        let patterns = [#"👤\s*(\d+)"#, #"[Ss]eeders?:?\s*(\d+)"#, #"\bS:?\s*(\d+)"#]
+        // FIX: the bare "S" pattern requires the colon now. The old #"\bS:?\s*(\d+)"#
+        // matched the "S01" of every "S01E02" episode tag, reporting 1 seeder and
+        // wrongly flagging healthy streams with the low-seeders warning.
+        let patterns = [#"👤\s*(\d+)"#, #"[Ss]eeders?:?\s*(\d+)"#, #"\bS:\s*(\d+)"#]
         for p in patterns {
             if let r = s.range(of: p, options: .regularExpression) {
                 let digits = s[r].filter(\.isNumber)
diff --git a/Nova/Services/TMDBClient.swift b/Nova/Services/TMDBClient.swift
index 3fac26d..be9d08c 100644
--- a/Nova/Services/TMDBClient.swift
+++ b/Nova/Services/TMDBClient.swift
@@ -396,11 +396,20 @@ actor TMDBClient {
         }
     }
 
-    private nonisolated func parseDate(_ s: String?) -> Date? {
-        guard let s, !s.isEmpty else { return nil }
+    // FIX: cache the air-date formatter. It was being re-created for every episode
+    // of every season during series hydration (a hot path); DateFormatter creation
+    // is expensive, and DateFormatter is documented thread-safe on modern OSes.
+    // DateFormatter isn't `Sendable`; this one is fully configured in its initializer
+    // and only ever read, so opt out of the concurrency check explicitly.
+    nonisolated(unsafe) private static let airDateFormatter: DateFormatter = {
         let f = DateFormatter()
         f.dateFormat = "yyyy-MM-dd"
         f.locale = Locale(identifier: "en_US_POSIX")
-        return f.date(from: s)
+        return f
+    }()
+
+    private nonisolated func parseDate(_ s: String?) -> Date? {
+        guard let s, !s.isEmpty else { return nil }
+        return Self.airDateFormatter.date(from: s)
     }
 }
diff --git a/Nova/Views/AIView.swift b/Nova/Views/AIView.swift
index fca1fb1..3749aee 100644
--- a/Nova/Views/AIView.swift
+++ b/Nova/Views/AIView.swift
@@ -119,8 +119,10 @@ struct AIView: View {
             .navigationDestination(for: CatalogItem.self) { item in
                 ContentDetailView(item: item)
             }
-            .navigationDestination(item: $playerItem) { item in
-                PlayerView(item: item)
+            .fullScreenCover(item: $playerItem) { item in
+                // Present the player as a full-screen cover so no tab bar, sidebar,
+                // or mini-bar remains visible during playback on any platform.
+                NavigationStack { PlayerView(item: item) }
             }
         }
     }
@@ -265,6 +267,8 @@ struct AIView: View {
                     Image(systemName: "xmark.circle.fill").foregroundStyle(Theme.Colors.textTertiary)
                 }
                 .buttonStyle(.plain)
+                // Accessibility: icon-only clear button needs a spoken name.
+                .accessibilityLabel("Clear prompt")
             }
             Button { run() } label: {
                 Text("Go").font(.appFont(18, weight: .bold))
@@ -293,7 +297,9 @@ struct AIView: View {
                             .padding(.vertical, Theme.Spacing.sm)
                             .background(Theme.Colors.card, in: Capsule())
                     }
-                    .buttonStyle(.plain)
+                    // Chip style gives these a proper tvOS focus/press effect,
+                    // matching every other chip in the app.
+                    .buttonStyle(NovaChipButtonStyle())
                 }
             }
         }
diff --git a/Nova/Views/Catalog/AddonsView.swift b/Nova/Views/Catalog/AddonsView.swift
index 30aa796..e890702 100644
--- a/Nova/Views/Catalog/AddonsView.swift
+++ b/Nova/Views/Catalog/AddonsView.swift
@@ -297,6 +297,8 @@ struct AddonsView: View {
                 set: { store.setEnabled(addon, $0) }
             ))
             .labelsHidden()
+            // Accessibility: the visual label is hidden, so name the toggle for VoiceOver.
+            .accessibilityLabel("\(addon.name) enabled")
 
             FocusableButton(title: "Remove", systemImage: "trash") {
                 store.remove(addon)
diff --git a/Nova/Views/Catalog/ContentDetailView.swift b/Nova/Views/Catalog/ContentDetailView.swift
index 1e96bff..42d7cc0 100644
--- a/Nova/Views/Catalog/ContentDetailView.swift
+++ b/Nova/Views/Catalog/ContentDetailView.swift
@@ -48,8 +48,13 @@ struct ContentDetailView: View {
         GeometryReader { proxy in
             ScrollView {
                 VStack(alignment: .leading, spacing: Theme.Spacing.md) {
-                    heroHeader
-                        .frame(width: proxy.size.width, alignment: .center)
+                    // The hero column is handed the concrete container width rather
+                    // than being wrapped in a `.frame(width:)`. A frame does not clamp
+                    // an over-wide child — it just centers it and lets it hang off
+                    // both edges — which is how the description and the action rail
+                    // kept ending up cut off. Passing the width down means every
+                    // child is proposed a width that actually fits.
+                    heroHeader(width: proxy.size.width)
 
                     if item.isSeries {
                         seriesBody
@@ -119,7 +124,7 @@ struct ContentDetailView: View {
         #endif
     }
 
-    private var heroHeader: some View {
+    private func heroHeader(width: CGFloat) -> some View {
         VStack(alignment: .center, spacing: Theme.Spacing.sm) {
             // Backdrop with a fade to the background at the bottom. Kept in the normal
             // scroll flow (no safe-area escape) so the content below follows directly
@@ -180,6 +185,8 @@ struct ContentDetailView: View {
                         .overlay(Circle().strokeBorder(Color.white.opacity(0.25), lineWidth: 1))
                 }
                 .buttonStyle(NovaChipButtonStyle())
+                // Accessibility: icon-only toggle needs a spoken name reflecting its state.
+                .accessibilityLabel(isWatched ? "Mark as unwatched" : "Mark as watched")
             }
 
             // Overview + year.
@@ -200,7 +207,9 @@ struct ContentDetailView: View {
             secondaryActionsRail
                 .padding(.top, Theme.Spacing.xs)
         }
-        .frame(maxWidth: .infinity)
+        // A concrete width (not `maxWidth: .infinity`) so the overview text and the
+        // action rail are always proposed the real container width and wrap inside it.
+        .frame(width: width)
     }
 
     /// "TV Show · Comedy · Animation" style line.
diff --git a/Nova/Views/Catalog/DiscoverView.swift b/Nova/Views/Catalog/DiscoverView.swift
index 1d5d742..9947287 100644
--- a/Nova/Views/Catalog/DiscoverView.swift
+++ b/Nova/Views/Catalog/DiscoverView.swift
@@ -182,6 +182,8 @@ struct DiscoverView: View {
                         .foregroundStyle(Theme.Colors.textTertiary)
                 }
                 .novaIconStyle()
+                // Accessibility: icon-only clear button needs a spoken name.
+                .accessibilityLabel("Clear search")
             }
             // AI (natural-language) search via the user's Worker.
             Button {
@@ -192,6 +194,8 @@ struct DiscoverView: View {
                     .foregroundStyle(aiSearching ? Theme.Colors.textTertiary : Theme.Colors.accent)
             }
             .novaIconStyle()
+            // Accessibility: name the sparkles icon so its purpose is clear.
+            .accessibilityLabel("AI search")
             .disabled(aiSearching || query.trimmingCharacters(in: .whitespaces).count < 2)
         }
         .padding(Theme.Spacing.md)
@@ -286,7 +290,8 @@ struct DiscoverView: View {
     private var hint: some View {
         Text("Search for a movie or show to find streams from your installed addons.")
             .font(.appFont(22))
-            .foregroundStyle(Theme.Colors.textTertiary)
+            // Contrast: this is primary guidance, not a footnote — tertiary (42%) is too dim.
+            .foregroundStyle(Theme.Colors.textSecondary)
             .padding(.top, Theme.Spacing.md)
     }
 
diff --git a/Nova/Views/Catalog/PersonView.swift b/Nova/Views/Catalog/PersonView.swift
index 5196400..0a338a9 100644
--- a/Nova/Views/Catalog/PersonView.swift
+++ b/Nova/Views/Catalog/PersonView.swift
@@ -25,7 +25,8 @@ struct PersonView: View {
                 header
 
                 if loading {
-                    ProgressView().tint(Theme.Colors.accent)
+                    // Use the shared LoadingView so loading states look consistent app-wide.
+                    LoadingView(message: "Loading filmography…")
                         .frame(maxWidth: .infinity, minHeight: 240)
                 } else if credits.isEmpty {
                     EmptyStateView(systemImage: "person.crop.rectangle",
@@ -99,5 +100,7 @@ struct PersonView: View {
             .foregroundStyle(Theme.Colors.textTertiary)
         }
         .frame(maxWidth: Theme.CardSize.posterWidth * 0.95)
+        // Accessibility: read poster, title, and year as one element per card.
+        .accessibilityElement(children: .combine)
     }
 }
diff --git a/Nova/Views/Catalog/StreamPickerView.swift b/Nova/Views/Catalog/StreamPickerView.swift
index 786f04b..6aad1e0 100644
--- a/Nova/Views/Catalog/StreamPickerView.swift
+++ b/Nova/Views/Catalog/StreamPickerView.swift
@@ -71,14 +71,18 @@ struct StreamPickerView: View {
                 content
             }
         }
-        .navigationDestination(item: $playable) { item in
-            PlayerView(item: item, series: catalog.isSeries ? catalog : nil, onStreamExpired: {
-                // The stream the player was using died. Mark it and auto-play the next.
-                if let dead = lastPlayedStream { markDead(dead) }
-                if let next = nextCandidate() {
-                    Task { await play(next) }
-                }
-            })
+        .fullScreenCover(item: $playable) { item in
+            // Present the player as a full-screen cover so no tab bar, sidebar,
+            // or mini-bar remains visible during playback on any platform.
+            NavigationStack {
+                PlayerView(item: item, series: catalog.isSeries ? catalog : nil, onStreamExpired: {
+                    // The stream the player was using died. Mark it and auto-play the next.
+                    if let dead = lastPlayedStream { markDead(dead) }
+                    if let next = nextCandidate() {
+                        Task { await play(next) }
+                    }
+                })
+            }
         }
         .navigationDestination(isPresented: $showAddonsSetup) {
             AddonsView()
@@ -417,6 +421,8 @@ struct StreamPickerView: View {
                                 .foregroundStyle(Theme.Colors.textTertiary)
                         }
                         .buttonStyle(.plain)
+                        // Accessibility: icon-only clear button needs a spoken name.
+                        .accessibilityLabel("Clear smart filter")
                     }
                 }
                 .padding(Theme.Spacing.sm)
@@ -486,11 +492,13 @@ struct StreamPickerView: View {
             Text(label)
                 .font(.appFont(17, weight: .semibold))
                 .padding(.horizontal, 14).padding(.vertical, 8)
-                .background(active ? Theme.Colors.accent : Color.white.opacity(0.10),
+                // Use the shared card color; chip style adds the tvOS focus effect
+                // so these match the quality chips above.
+                .background(active ? Theme.Colors.accent : Theme.Colors.card,
                             in: Capsule())
                 .foregroundStyle(active ? .white : Theme.Colors.textPrimary)
         }
-        .buttonStyle(.plain)
+        .buttonStyle(NovaChipButtonStyle())
     }
 
     /// The remembered stream entry for this movie/episode, if any.
diff --git a/Nova/Views/DirectURL/DirectURLView.swift b/Nova/Views/DirectURL/DirectURLView.swift
index d690491..0d19f7e 100644
--- a/Nova/Views/DirectURL/DirectURLView.swift
+++ b/Nova/Views/DirectURL/DirectURLView.swift
@@ -37,9 +37,9 @@ struct DirectURLView: View {
                         .font(.appFont(22))
                         .foregroundStyle(Theme.Colors.textSecondary)
 
-                    field("Video URL", text: $urlText, placeholder: "https://example.com/video.mp4")
+                    field("Video URL", text: $urlText, placeholder: "https://example.com/video.mp4", isURL: true)
                     field("Title (optional)", text: $titleText, placeholder: "My Video")
-                    field("Poster URL (optional)", text: $posterText, placeholder: "https://example.com/poster.jpg")
+                    field("Poster URL (optional)", text: $posterText, placeholder: "https://example.com/poster.jpg", isURL: true)
 
                     if settings.requireLegalConfirmation {
                         LegalConfirmToggle(isOn: $legalConfirmed)
@@ -73,8 +73,10 @@ struct DirectURLView: View {
                 .frame(maxWidth: Theme.contentMaxWidth(1100), alignment: .leading)
             }
         }
-        .navigationDestination(item: $addedItem) { item in
-            PlayerView(item: item)
+        .fullScreenCover(item: $addedItem) { item in
+            // Present the player as a full-screen cover so no tab bar, sidebar,
+            // or mini-bar remains visible during playback on any platform.
+            NavigationStack { PlayerView(item: item) }
         }
         .dismissKeyboardOnTap()
     }
@@ -87,7 +89,7 @@ struct DirectURLView: View {
 
     // MARK: - Field
 
-    private func field(_ label: String, text: Binding<String>, placeholder: String) -> some View {
+    private func field(_ label: String, text: Binding<String>, placeholder: String, isURL: Bool = false) -> some View {
         VStack(alignment: .leading, spacing: Theme.Spacing.xs) {
             Text(label)
                 .font(.appFont(20, weight: .semibold))
@@ -97,6 +99,10 @@ struct DirectURLView: View {
                     .textFieldStyle(.plain)
                     #if os(iOS)
                     .textSelection(.enabled)
+                    // URL fields get the URL keyboard and skip auto-caps/correction.
+                    .keyboardType(isURL ? .URL : .default)
+                    .textInputAutocapitalization(isURL ? .never : .sentences)
+                    .autocorrectionDisabled(isURL)
                     #endif
                     .padding(Theme.Spacing.md)
                     .background(Theme.Colors.card, in: RoundedRectangle(cornerRadius: Theme.Radius.button, style: .continuous))
@@ -121,6 +127,8 @@ struct DirectURLView: View {
         library.add(item)
         // Reset the form on success.
         urlText = ""; titleText = ""; posterText = ""; legalConfirmed = false
+        // The form clears silently, so confirm the add with a toast.
+        ToastCenter.shared.show("Added “\(item.title)” to your library")
     }
 
     /// Validates and constructs a MediaItem, surfacing any error inline.
diff --git a/Nova/Views/Home/HomeView.swift b/Nova/Views/Home/HomeView.swift
index 2523317..9b963b3 100644
--- a/Nova/Views/Home/HomeView.swift
+++ b/Nova/Views/Home/HomeView.swift
@@ -49,8 +49,10 @@ struct HomeView: View {
                     watchNowContent
                 }
             }
-            .navigationDestination(item: $selectedItem) { item in
-                PlayerView(item: item)
+            .fullScreenCover(item: $selectedItem) { item in
+                // Present the player as a full-screen cover so no tab bar, sidebar,
+                // or mini-bar remains visible during playback on any platform.
+                NavigationStack { PlayerView(item: item) }
             }
             .navigationDestination(for: CatalogItem.self) { item in
                 ContentDetailView(item: item)
@@ -239,6 +241,8 @@ struct HomeView: View {
                         }
                     }
                     .animation(.easeOut(duration: 0.18), value: heroIndex)
+                    // Accessibility: the dots are decorative; VoiceOver reads the hero itself.
+                    .accessibilityHidden(true)
                 }
             }
             .task(id: heroIndex) {
@@ -271,6 +275,9 @@ struct HomeView: View {
                 .background(.ultraThinMaterial, in: Circle())
         }
         .buttonStyle(NovaListRowStyle())
+        // Accessibility: icon-only button needs a spoken name and purpose.
+        .accessibilityLabel("Customize Home")
+        .accessibilityHint("Choose which shelves appear on Home")
         .padding(.horizontal, Theme.Spacing.edge)
         .padding(.top, Theme.Spacing.sm)
         #if os(tvOS)
@@ -497,6 +504,8 @@ struct QueueManageView: View {
                                 }
                                 Spacer()
                             }
+                            // Accessibility: read poster, title, and status as one element.
+                            .accessibilityElement(children: .combine)
                             .listRowBackground(Theme.Colors.card)
                         }
                         .onMove { library.moveInQueue(from: $0, to: $1) }
diff --git a/Nova/Views/Library/CollectionsView.swift b/Nova/Views/Library/CollectionsView.swift
index 60b1440..2c5bf18 100644
--- a/Nova/Views/Library/CollectionsView.swift
+++ b/Nova/Views/Library/CollectionsView.swift
@@ -67,8 +67,10 @@ struct CollectionsView: View {
             }
         }
         .navigationTitle("Collections")
-        .navigationDestination(item: $selectedItem) { item in
-            PlayerView(item: item)
+        .fullScreenCover(item: $selectedItem) { item in
+            // Present the player as a full-screen cover so no tab bar, sidebar,
+            // or mini-bar remains visible during playback on any platform.
+            NavigationStack { PlayerView(item: item) }
         }
         .toolbar {
             if !library.collections.isEmpty {
diff --git a/Nova/Views/Library/DuplicatesView.swift b/Nova/Views/Library/DuplicatesView.swift
index f339720..5f2d521 100644
--- a/Nova/Views/Library/DuplicatesView.swift
+++ b/Nova/Views/Library/DuplicatesView.swift
@@ -35,7 +35,11 @@ struct DuplicatesView: View {
                             .padding(.horizontal, Theme.Spacing.edge)
 
                         FocusableButton(title: "Merge All", systemImage: "arrow.triangle.merge", prominent: true) {
+                            let count = groups.count
                             withAnimation { library.mergeAllDuplicates(); rescan() }
+                            // Merging succeeds silently otherwise — confirm what happened.
+                            ToastCenter.shared.show("Merged \(count) duplicate group\(count == 1 ? "" : "s")",
+                                                    systemImage: "arrow.triangle.merge")
                         }
                         .frame(maxWidth: 320)
                         .padding(.horizontal, Theme.Spacing.edge)
@@ -103,6 +107,8 @@ struct DuplicatesView: View {
             }
             FocusableButton(title: "Merge These", systemImage: "arrow.triangle.merge") {
                 withAnimation { library.mergeDuplicates(group); rescan() }
+                // Confirm the merge, since the card just disappears from the list.
+                ToastCenter.shared.show("Merged “\(group.title)”", systemImage: "arrow.triangle.merge")
             }
             .frame(maxWidth: 280)
             .padding(.top, Theme.Spacing.xs)
diff --git a/Nova/Views/Library/LibraryView.swift b/Nova/Views/Library/LibraryView.swift
index 05473fc..9abfe6e 100644
--- a/Nova/Views/Library/LibraryView.swift
+++ b/Nova/Views/Library/LibraryView.swift
@@ -120,8 +120,10 @@ struct LibraryView: View {
             .sheet(isPresented: $showStats) {
                 NavigationStack { WatchStatsView() }
             }
-            .navigationDestination(item: $selectedItem) { item in
-                PlayerView(item: item)
+            .fullScreenCover(item: $selectedItem) { item in
+                // Present the player as a full-screen cover so no tab bar, sidebar,
+                // or mini-bar remains visible during playback on any platform.
+                NavigationStack { PlayerView(item: item) }
             }
             // Present the library item detail as a full navigation push on every
             // platform, matching how Home and Discover open detail. On iPad a plain
diff --git a/Nova/Views/LiveTV/LiveTVSourcesView.swift b/Nova/Views/LiveTV/LiveTVSourcesView.swift
index f1998cc..53a99e5 100644
--- a/Nova/Views/LiveTV/LiveTVSourcesView.swift
+++ b/Nova/Views/LiveTV/LiveTVSourcesView.swift
@@ -14,6 +14,8 @@ private struct LiveTVSourcesContent: View {
     @EnvironmentObject private var env: AppEnvironment
     @ObservedObject var store: LiveTVSourceStore
     @State private var showAdd = false
+    // Playlist pending delete confirmation, so a stray tap can't remove it.
+    @State private var pendingDelete: LiveTVSource?
 
     var body: some View {
         ZStack {
@@ -54,8 +56,23 @@ private struct LiveTVSourcesContent: View {
                 store.addCustom(name: name, url: url, kind: kind, username: user, password: pass,
                                 epgURL: epg, refreshHours: hours)
                 showAdd = false
+                // Confirm the silent add so the user knows the playlist was saved.
+                ToastCenter.shared.show("Playlist added", systemImage: "dot.radiowaves.left.and.right")
             }.environmentObject(env)
         }
+        // Destructive confirmation before removing a custom playlist.
+        .alert("Remove Playlist?",
+               isPresented: Binding(get: { pendingDelete != nil },
+                                    set: { if !$0 { pendingDelete = nil } }),
+               presenting: pendingDelete) { source in
+            Button("Remove “\(source.name)”", role: .destructive) {
+                store.remove(source)
+                pendingDelete = nil
+            }
+            Button("Cancel", role: .cancel) { pendingDelete = nil }
+        } message: { source in
+            Text("“\(source.name)” and its channels will be removed from Live TV.")
+        }
     }
 
     private func sourceRow(_ source: LiveTVSource) -> some View {
@@ -75,7 +92,8 @@ private struct LiveTVSourcesContent: View {
             Toggle("", isOn: Binding(get: { source.isEnabled }, set: { store.setEnabled($0, for: source) }))
                 .labelsHidden().tint(Theme.Colors.accent)
             if !source.isBuiltIn {
-                Button(role: .destructive) { store.remove(source) } label: {
+                // Ask before deleting instead of removing immediately.
+                Button(role: .destructive) { pendingDelete = source } label: {
                     Image(systemName: "trash").foregroundStyle(Theme.Colors.error)
                 }.buttonStyle(.plain)
             }
diff --git a/Nova/Views/LiveTV/LiveTVView.swift b/Nova/Views/LiveTV/LiveTVView.swift
index da7bb78..1cb21bb 100644
--- a/Nova/Views/LiveTV/LiveTVView.swift
+++ b/Nova/Views/LiveTV/LiveTVView.swift
@@ -54,6 +54,13 @@ struct LiveTVView: View {
                     }
                     .padding(.vertical, Theme.Spacing.lg)
                 }
+                #if os(iOS)
+                // Pull-to-refresh reloads playlists and the programme guide (touch only).
+                .refreshable {
+                    await env.liveTVSources.refreshAll()
+                    await loadEPG()
+                }
+                #endif
             }
 
             if resolving {
@@ -62,8 +69,10 @@ struct LiveTVView: View {
                     .background(.black.opacity(0.5))
             }
         }
-        .navigationDestination(item: $playable) { item in
-            PlayerView(item: item)
+        .fullScreenCover(item: $playable) { item in
+            // Present the player as a full-screen cover so no tab bar, sidebar,
+            // or mini-bar remains visible during playback on any platform.
+            NavigationStack { PlayerView(item: item) }
         }
         .alert("Couldn't play channel", isPresented: .constant(errorMessage != nil)) {
             Button("OK") { errorMessage = nil }
diff --git a/Nova/Views/Magnet/MagnetView.swift b/Nova/Views/Magnet/MagnetView.swift
index 7c0abc7..e4f3543 100644
--- a/Nova/Views/Magnet/MagnetView.swift
+++ b/Nova/Views/Magnet/MagnetView.swift
@@ -64,8 +64,10 @@ struct MagnetView: View {
                 .frame(maxWidth: Theme.contentMaxWidth(1100), alignment: .leading)
             }
         }
-        .navigationDestination(item: $addedItem) { item in
-            PlayerView(item: item)
+        .fullScreenCover(item: $addedItem) { item in
+            // Present the player as a full-screen cover so no tab bar, sidebar,
+            // or mini-bar remains visible during playback on any platform.
+            NavigationStack { PlayerView(item: item) }
         }
         .dismissKeyboardOnTap()
     }
@@ -96,6 +98,8 @@ struct MagnetView: View {
                     .padding(Theme.Spacing.md)
                     .background(Theme.Colors.card, in: RoundedRectangle(cornerRadius: Theme.Radius.button, style: .continuous))
                     .foregroundStyle(Theme.Colors.textPrimary)
+                    // Keyboard return submits when the magnet is valid and confirmed.
+                    .onSubmit { if canSubmit { Task { await submit() } } }
                 #if os(iOS)
                 PasteButton(text: $magnetText)
                 #endif
diff --git a/Nova/Views/Player/PlayerView.swift b/Nova/Views/Player/PlayerView.swift
index 6be52fe..1117d67 100644
--- a/Nova/Views/Player/PlayerView.swift
+++ b/Nova/Views/Player/PlayerView.swift
@@ -67,18 +67,28 @@ struct PlayerView: View {
             #endif
         }
         .overlay {
-            if let pos = resumePromptPosition {
-                resumeRestartPrompt(position: pos)
+            // Container so the prompt springs in with scale + fade instead of popping.
+            ZStack {
+                if let pos = resumePromptPosition {
+                    resumeRestartPrompt(position: pos)
+                        .transition(.scale(scale: 0.96).combined(with: .opacity))
+                }
             }
+            .animation(Theme.Motion.spring, value: resumePromptPosition == nil)
         }
         .overlay {
             // Night mode: a gentle dark veil over the video to ease late viewing.
             // Doesn't intercept taps/focus so playback controls still work.
-            if settings.nightMode {
-                Color.black.opacity(0.22)
-                    .ignoresSafeArea()
-                    .allowsHitTesting(false)
+            // Fades in/out when toggled rather than snapping.
+            ZStack {
+                if settings.nightMode {
+                    Color.black.opacity(0.22)
+                        .ignoresSafeArea()
+                        .transition(.opacity)
+                }
             }
+            .animation(.easeInOut(duration: 0.35), value: settings.nightMode)
+            .allowsHitTesting(false)
         }
     }
 
@@ -101,6 +111,8 @@ struct PlayerView: View {
                 VStack(spacing: Theme.Spacing.sm) {
                     FocusableButton(title: "Resume from \(timeLabel(position))",
                                     systemImage: "play.fill", prominent: true) {
+                        // Light tap on resuming playback (no-op on tvOS).
+                        Haptics.impact(.light)
                         didChooseResume = true
                         resumePromptPosition = nil
                         model.forceRestart = false
@@ -324,12 +336,22 @@ struct PlayerView: View {
                 onRefresh: { Task { await model.refreshSubtitlesFromProviders() } },
                 onSelect: { model.selectSubtitle($0) }
             )
+            #if os(iOS)
+            // Half-height by default with a grab handle, like system pickers.
+            .presentationDetents([.medium, .large])
+            .presentationDragIndicator(.visible)
+            #endif
         }
         #if os(iOS)
         .navigationBarBackButtonHidden(true)
         .toolbar(.hidden, for: .navigationBar)
         .toolbar(.hidden, for: .tabBar)
         .statusBarHidden(true)
+        // True fullscreen: also dim the home indicator and keep the screen awake
+        // for the duration of playback.
+        .persistentSystemOverlays(.hidden)
+        .onAppear { UIApplication.shared.isIdleTimerDisabled = true }
+        .onDisappear { UIApplication.shared.isIdleTimerDisabled = false }
         #endif
         .onChange(of: model.didFinish) { _, finished in
             if finished { handleFinish() }
diff --git a/Nova/Views/Player/SubtitlePickerView.swift b/Nova/Views/Player/SubtitlePickerView.swift
index d6199fc..baebbeb 100644
--- a/Nova/Views/Player/SubtitlePickerView.swift
+++ b/Nova/Views/Player/SubtitlePickerView.swift
@@ -129,7 +129,11 @@ struct SubtitlePickerView: View {
 
     private func row(title: String, subtitle: String?, isSelected: Bool,
                      action: @escaping () -> Void) -> some View {
-        Button(action: action) {
+        Button {
+            // Success haptic when a subtitle choice is applied (no-op on tvOS).
+            Haptics.play(.success)
+            action()
+        } label: {
             HStack(spacing: Theme.Spacing.md) {
                 Image(systemName: isSelected ? "checkmark.circle.fill" : "circle")
                     .foregroundStyle(isSelected ? Theme.Colors.accent : Theme.Colors.textSecondary)
diff --git a/Nova/Views/Player/VLCPlayerView.swift b/Nova/Views/Player/VLCPlayerView.swift
index 70e15bc..d02444c 100644
--- a/Nova/Views/Player/VLCPlayerView.swift
+++ b/Nova/Views/Player/VLCPlayerView.swift
@@ -186,18 +186,33 @@ struct VLCPlayerView: View {
             if phase != .active { model.checkpointProgress() }
         }
         .overlay {
-            if let pos = resumePromptPosition {
-                resumeRestartPrompt(position: pos)
+            // Container so the prompt springs in with scale + fade instead of popping.
+            ZStack {
+                if let pos = resumePromptPosition {
+                    resumeRestartPrompt(position: pos)
+                        .transition(.scale(scale: 0.96).combined(with: .opacity))
+                }
             }
+            .animation(Theme.Motion.spring, value: resumePromptPosition == nil)
         }
         #if os(iOS)
         .statusBarHidden(true)
         .navigationBarBackButtonHidden(true)
         .toolbar(.hidden, for: .navigationBar)
         .toolbar(.hidden, for: .tabBar)
+        // True fullscreen: also dim the home indicator and keep the screen awake
+        // for the duration of playback.
+        .persistentSystemOverlays(.hidden)
+        .onAppear { UIApplication.shared.isIdleTimerDisabled = true }
+        .onDisappear { UIApplication.shared.isIdleTimerDisabled = false }
         #endif
         .sheet(isPresented: $model.showSubtitlePicker) {
             trackPicker
+            #if os(iOS)
+            // Half-height by default with a grab handle, like system pickers.
+            .presentationDetents([.medium, .large])
+            .presentationDragIndicator(.visible)
+            #endif
         }
         .navigationDestination(item: $navigateNext) { next in
             VLCPlayerView(item: next, series: series)
@@ -311,6 +326,8 @@ struct VLCPlayerView: View {
                         }
 
                         Button {
+                            // Light tap on play/pause (no-op on tvOS).
+                            Haptics.impact(.light)
                             model.togglePlayPause(); revealControls()
                         } label: {
                             Image(systemName: model.isPlaying ? "pause.fill" : "play.fill")
@@ -526,6 +543,8 @@ struct VLCPlayerView: View {
                         controlButton("gobackward.15") { model.skipBackward() }
                     }
                     controlButton(model.isPlaying ? "pause.fill" : "play.fill", large: true) {
+                        // Light tap on play/pause (no-op on tvOS).
+                        Haptics.impact(.light)
                         model.togglePlayPause(); revealControls()
                     }
                     #if os(tvOS)
diff --git a/Nova/Views/RealDebrid/RealDebridView.swift b/Nova/Views/RealDebrid/RealDebridView.swift
index e59762c..54f3fe6 100644
--- a/Nova/Views/RealDebrid/RealDebridView.swift
+++ b/Nova/Views/RealDebrid/RealDebridView.swift
@@ -28,6 +28,8 @@ struct RealDebridView: View {
     @State private var linkText = ""
     @State private var legalConfirmed = false
     @State private var addedItem: MediaItem?
+    // Confirms sign-out before the token is actually deleted.
+    @State private var confirmingRemoveToken = false
 
     private var hasToken: Bool { KeychainStore.shared.realDebridToken != nil }
 
@@ -53,8 +55,10 @@ struct RealDebridView: View {
                 .frame(maxWidth: Theme.contentMaxWidth(1100), alignment: .leading)
             }
         }
-        .navigationDestination(item: $addedItem) { item in
-            PlayerView(item: item)
+        .fullScreenCover(item: $addedItem) { item in
+            // Present the player as a full-screen cover so no tab bar, sidebar,
+            // or mini-bar remains visible during playback on any platform.
+            NavigationStack { PlayerView(item: item) }
         }
         .task { await loadAccountIfPossible() }
         .dismissKeyboardOnTap()
@@ -84,12 +88,21 @@ struct RealDebridView: View {
                     }
                 }
                 FocusableButton(title: "Remove Token", systemImage: "trash") {
-                    try? KeychainStore.shared.clearRealDebridToken()
-                    user = nil
-                    statusMessage = "Token removed."
-                    isError = false
+                    // Ask first — removing the token signs the account out.
+                    confirmingRemoveToken = true
                 }
                 .frame(maxWidth: Theme.isCompact ? .infinity : 300)
+                .alert("Remove Real-Debrid Token?", isPresented: $confirmingRemoveToken) {
+                    Button("Remove", role: .destructive) {
+                        try? KeychainStore.shared.clearRealDebridToken()
+                        user = nil
+                        statusMessage = "Token removed."
+                        isError = false
+                    }
+                    Button("Cancel", role: .cancel) {}
+                } message: {
+                    Text("You'll be signed out of Real-Debrid on this device until you connect again.")
+                }
             } else if let device = rdDeviceCode {
                 // Browser device-code flow in progress.
                 VStack(alignment: .leading, spacing: Theme.Spacing.md) {
diff --git a/Nova/Views/RootView.swift b/Nova/Views/RootView.swift
index ccc1054..c915e3b 100644
--- a/Nova/Views/RootView.swift
+++ b/Nova/Views/RootView.swift
@@ -29,9 +29,13 @@ struct RootView: View {
             .tint(accentManager.accent)
             .onOpenURL { nav.handle(url: $0) }
             .fullScreenCover(item: $reopenItem) { item in
-                PlayerView(item: item, autoResume: true)
-                    .environmentObject(env)
-                    .environmentObject(nav)
+                // NavigationStack so the player's internal next-episode navigation
+                // works, and the cover guarantees nothing else is visible behind it.
+                NavigationStack {
+                    PlayerView(item: item, autoResume: true)
+                }
+                .environmentObject(env)
+                .environmentObject(nav)
             }
             .toastHost()
             .fullScreenCover(isPresented: Binding(
@@ -199,8 +203,22 @@ struct RootView: View {
 
     // MARK: - Now Playing mini-player
 
-    @ViewBuilder
+    /// Whether the mini bar is currently on screen, driving its show/hide animation.
+    private var miniBarVisible: Bool {
+        nowPlaying.current != nil && reopenItem == nil && !nowPlaying.playerPresented
+    }
+
     private var nowPlayingBar: some View {
+        // ZStack container so the bar's insertion/removal transition is animated
+        // (springs in/out) instead of snapping when playback state flips.
+        ZStack {
+            nowPlayingBarContent
+        }
+        .animation(Theme.Motion.spring, value: miniBarVisible)
+    }
+
+    @ViewBuilder
+    private var nowPlayingBarContent: some View {
         if let item = nowPlaying.current, reopenItem == nil, !nowPlaying.playerPresented {
             let isTV = PlatformCapabilities.platform == .appleTV
             VStack(spacing: 0) {
@@ -222,6 +240,9 @@ struct RootView: View {
                                     .font(.appFont(isTV ? 16 : 13))
                                     .foregroundStyle(.white.opacity(0.68))
                                     .lineLimit(1)
+                                    // Roll the percent digits instead of hard-swapping.
+                                    .contentTransition(.numericText())
+                                    .animation(.easeInOut(duration: 0.3), value: nowPlaying.progress)
                             }
                             Spacer(minLength: Theme.Spacing.sm)
                         }
@@ -229,7 +250,11 @@ struct RootView: View {
                     }
                     .buttonStyle(.plain)
 
-                    Button { reopenItem = item } label: {
+                    Button {
+                        // Light tap confirms resuming playback (no-op on tvOS).
+                        Haptics.impact(.light)
+                        reopenItem = item
+                    } label: {
                         Image(systemName: "play.fill")
                             .font(.appFont(isTV ? 24 : 18, weight: .semibold))
                             .foregroundStyle(.black)
@@ -258,6 +283,8 @@ struct RootView: View {
                             .frame(width: geo.size.width * nowPlaying.progress)
                     }
                 }
+                // Glide the progress capsule to new widths instead of jumping.
+                .animation(.linear(duration: 0.3), value: nowPlaying.progress)
                 .frame(height: isTV ? 5 : 3)
                 .padding(.horizontal, isTV ? Theme.Spacing.md : 12)
                 .padding(.bottom, isTV ? 12 : 8)
diff --git a/Nova/Views/SMB/SMBBrowseView.swift b/Nova/Views/SMB/SMBBrowseView.swift
index ff9d4e1..35bf00b 100644
--- a/Nova/Views/SMB/SMBBrowseView.swift
+++ b/Nova/Views/SMB/SMBBrowseView.swift
@@ -48,8 +48,10 @@ struct SMBBrowseView: View {
                 content
             }
         }
-        .navigationDestination(item: $selectedItem) { item in
-            PlayerView(item: item)
+        .fullScreenCover(item: $selectedItem) { item in
+            // Present the player as a full-screen cover so no tab bar, sidebar,
+            // or mini-bar remains visible during playback on any platform.
+            NavigationStack { PlayerView(item: item) }
         }
         .task { await load() }
     }
@@ -281,6 +283,8 @@ struct SMBBrowseView: View {
         do {
             let item = try await makeItem(for: file)
             library.add(item)
+            // The row doesn't change on success, so confirm the add with a toast.
+            ToastCenter.shared.show("Added “\(item.title)” to your library")
         } catch {
             state = .error((error as? LocalizedError)?.errorDescription ?? error.localizedDescription)
         }
diff --git a/Nova/Views/SMB/SMBListView.swift b/Nova/Views/SMB/SMBListView.swift
index a989e7c..c7249de 100644
--- a/Nova/Views/SMB/SMBListView.swift
+++ b/Nova/Views/SMB/SMBListView.swift
@@ -16,6 +16,8 @@ import Combine
 struct SMBListView: View {
     @StateObject private var model = SMBSharesModel()
     @State private var showingAdd = false
+    // Share pending delete confirmation, so a stray tap can't remove it.
+    @State private var pendingDelete: SMBShare?
 
     var body: some View {
         ZStack {
@@ -62,6 +64,20 @@ struct SMBListView: View {
                 ToastCenter.shared.show("Added \(newShare.displayName)", systemImage: "externaldrive.fill.badge.plus")
             }
         }
+        // Destructive confirmation before deleting a saved share (also clears its
+        // Keychain password), so a stray tap on the trash icon is recoverable.
+        .alert("Delete Share?",
+               isPresented: Binding(get: { pendingDelete != nil },
+                                    set: { if !$0 { pendingDelete = nil } }),
+               presenting: pendingDelete) { share in
+            Button("Delete “\(share.displayName)”", role: .destructive) {
+                model.remove(share)
+                pendingDelete = nil
+            }
+            Button("Cancel", role: .cancel) { pendingDelete = nil }
+        } message: { share in
+            Text("“\(share.displayName)” and its saved password will be removed.")
+        }
     }
 
     private func shareRow(_ share: SMBShare) -> some View {
@@ -102,7 +118,8 @@ struct SMBListView: View {
             .buttonStyle(.plain)
 
             Button {
-                model.remove(share)
+                // Ask before deleting instead of removing immediately.
+                pendingDelete = share
             } label: {
                 Image(systemName: "trash")
                     .font(.appFont(22))
diff --git a/Nova/Views/Settings/AccountsView.swift b/Nova/Views/Settings/AccountsView.swift
index 6daf404..78a927a 100644
--- a/Nova/Views/Settings/AccountsView.swift
+++ b/Nova/Views/Settings/AccountsView.swift
@@ -106,6 +106,9 @@ struct AccountsView: View {
                 }
                 .frame(maxWidth: Theme.isCompact ? .infinity : 360)
                 .padding(.top, Theme.Spacing.md)
+                // Nothing typed means nothing to save — disable to avoid a no-op tap.
+                .disabled(!hasInput)
+                .opacity(hasInput ? 1 : 0.5)
 
                 Text("Credentials are stored securely in the device Keychain.")
                     .font(.appFont(16))
@@ -177,6 +180,12 @@ struct AccountsView: View {
         }
     }
 
+    /// True when at least one credential field has text to save.
+    private var hasInput: Bool {
+        ![tmdbKey, traktID, traktSecret, openSubtitlesKey, omdbKey]
+            .allSatisfy { $0.trimmingCharacters(in: .whitespaces).isEmpty }
+    }
+
     private func loadExisting() {
         // We don't echo secrets back; fields stay blank unless the user types.
     }
@@ -189,6 +198,8 @@ struct AccountsView: View {
         if !omdbKey.isEmpty { config.set(omdbKey, for: .omdbAPIKey) }
 
         tmdbKey = ""; traktID = ""; traktSecret = ""; openSubtitlesKey = ""; omdbKey = ""
+        // Toast so the save is confirmed even after the fields clear.
+        ToastCenter.shared.show("Credentials saved", systemImage: "key.fill")
         savedFlash = true
         Task {
             try? await Task.sleep(nanoseconds: 1_500_000_000)
diff --git a/Nova/Views/Settings/LibraryFoldersView.swift b/Nova/Views/Settings/LibraryFoldersView.swift
index 842e0e0..2c79cfc 100644
--- a/Nova/Views/Settings/LibraryFoldersView.swift
+++ b/Nova/Views/Settings/LibraryFoldersView.swift
@@ -18,6 +18,8 @@ private struct LibraryFoldersContent: View {
     @ObservedObject var store: LibraryFolderStore
     @State private var showPicker = false
     @State private var lastResult: String?
+    // Folder pending remove confirmation, so a stray tap can't remove it.
+    @State private var pendingDelete: LibraryFolder?
 
     var body: some View {
         ZStack {
@@ -53,6 +55,19 @@ private struct LibraryFoldersContent: View {
             }
             .environmentObject(env)
         }
+        // Destructive confirmation before removing a scanned folder.
+        .alert("Remove Folder?",
+               isPresented: Binding(get: { pendingDelete != nil },
+                                    set: { if !$0 { pendingDelete = nil } }),
+               presenting: pendingDelete) { folder in
+            Button("Remove “\(folder.displayName)”", role: .destructive) {
+                store.removeFolder(folder)
+                pendingDelete = nil
+            }
+            Button("Cancel", role: .cancel) { pendingDelete = nil }
+        } message: { folder in
+            Text("“\(folder.displayName)” will no longer be scanned. Items already in your library stay.")
+        }
     }
 
     private var emptyState: some View {
@@ -95,7 +110,8 @@ private struct LibraryFoldersContent: View {
                     }
                 } label: { Label("Rescan", systemImage: "arrow.clockwise").font(.appFont(15, weight: .semibold)) }
                 .buttonStyle(NovaChipButtonStyle()).disabled(scanning)
-                Button(role: .destructive) { store.removeFolder(folder) } label: {
+                // Ask before removing instead of deleting immediately.
+                Button(role: .destructive) { pendingDelete = folder } label: {
                     Label("Remove", systemImage: "trash").font(.appFont(15, weight: .semibold)).foregroundStyle(Theme.Colors.error)
                 }
                 .buttonStyle(NovaChipButtonStyle()).disabled(scanning)
diff --git a/Nova/Views/Settings/TitleCleanupRulesView.swift b/Nova/Views/Settings/TitleCleanupRulesView.swift
index 194e67e..d748108 100644
--- a/Nova/Views/Settings/TitleCleanupRulesView.swift
+++ b/Nova/Views/Settings/TitleCleanupRulesView.swift
@@ -15,6 +15,8 @@ struct TitleCleanupRulesView: View {
     @State private var editing: TitleCleanupRule?
     @State private var showingNew = false
     @State private var preview = "The.Matrix.1999.1080p.BluRay.x264-GROUP"
+    // Confirms reset before custom rules are actually discarded.
+    @State private var confirmingReset = false
 
     var body: some View {
         List {
@@ -78,10 +80,17 @@ struct TitleCleanupRulesView: View {
                     Label("Add Rule", systemImage: "plus.circle.fill")
                 }
                 Button(role: .destructive) {
-                    store.resetToDefaults()
+                    // Ask first — resetting throws away any custom rules.
+                    confirmingReset = true
                 } label: {
                     Label("Reset to Defaults", systemImage: "arrow.counterclockwise")
                 }
+                .alert("Reset Cleanup Rules?", isPresented: $confirmingReset) {
+                    Button("Reset", role: .destructive) { store.resetToDefaults() }
+                    Button("Cancel", role: .cancel) {}
+                } message: {
+                    Text("All custom rules will be replaced with the built-in defaults.")
+                }
             }
         }
         .navigationTitle("Cleanup Rules")
diff --git a/Nova/Views/Settings/TraktConnectView.swift b/Nova/Views/Settings/TraktConnectView.swift
index 0c7b2e5..bb2c495 100644
--- a/Nova/Views/Settings/TraktConnectView.swift
+++ b/Nova/Views/Settings/TraktConnectView.swift
@@ -17,6 +17,8 @@ struct TraktConnectView: View {
 
     @AppStorage("settings.traktLastSync") private var lastSyncStamp: Double = 0
     @State private var syncing = false
+    // Confirms disconnect before the Trakt sign-out actually happens.
+    @State private var confirmingDisconnect = false
 
     @State private var phase: Phase = .checking
     @State private var deviceCode: TraktDeviceCode?
@@ -85,14 +87,23 @@ struct TraktConnectView: View {
             scrobbleControlPanel
 
             FocusableButton(title: "Disconnect", systemImage: "rectangle.portrait.and.arrow.right") {
-                Task {
-                    await env.trakt.signOut()
-                    phase = .notConfigured
-                    await onAppear()
-                }
+                // Ask first — disconnecting stops watchlist and progress sync.
+                confirmingDisconnect = true
             }
             .frame(maxWidth: Theme.isCompact ? .infinity : 320)
             .padding(.top, Theme.Spacing.sm)
+            .alert("Disconnect Trakt?", isPresented: $confirmingDisconnect) {
+                Button("Disconnect", role: .destructive) {
+                    Task {
+                        await env.trakt.signOut()
+                        phase = .notConfigured
+                        await onAppear()
+                    }
+                }
+                Button("Cancel", role: .cancel) {}
+            } message: {
+                Text("Your watchlist and watched progress will stop syncing until you connect again.")
+            }
         }
     }
 
@@ -165,6 +176,8 @@ struct TraktConnectView: View {
         await env.trakt.syncNow()
         lastSyncStamp = Date().timeIntervalSince1970
         syncing = false
+        // Visible completion feedback — the sync itself finishes silently.
+        ToastCenter.shared.show("Trakt sync complete", systemImage: "arrow.triangle.2.circlepath")
     }
 
     private var deviceCodeView: some View {
diff --git a/Nova/Views/TVMenuOverlay.swift b/Nova/Views/TVMenuOverlay.swift
index ca46ead..912a52e 100644
--- a/Nova/Views/TVMenuOverlay.swift
+++ b/Nova/Views/TVMenuOverlay.swift
@@ -91,8 +91,10 @@ private struct TVTabPillBody: View {
             )
             .shadow(color: isFocused ? Color.black.opacity(0.5) : .clear,
                     radius: isFocused ? 18 : 0, y: isFocused ? 8 : 0)
-            .scaleEffect(isFocused ? 1.06 : 1.0)
+            // Slight press-down while the remote click is held, on top of focus lift.
+            .scaleEffect(configuration.isPressed ? 0.97 : (isFocused ? 1.06 : 1.0))
             .animation(.easeOut(duration: 0.16), value: isFocused)
+            .animation(.easeOut(duration: 0.12), value: configuration.isPressed)
     }
 }
 #endif
